- Don't leave TODO or incomplete code implementation
- Use semantic search to find related files and functions
- Use Tailwind v4 CSS for styling
- Use Shadcn UI components, install them using `pnpm dlx shadcn@latest add <component-name>` if not available in `src/components/uiui`